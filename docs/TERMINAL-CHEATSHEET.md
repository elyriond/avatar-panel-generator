# Terminal-Spickzettel – nervensystemfreundlich 💛

Dieser Spickzettel ist für dich geschrieben: ruhig, klar, freundlich. Du musst nichts perfekt können. Kleine Schritte sind völlig okay. Du kannst jederzeit abbrechen und neu starten.

Hinweis: „Terminal“ (Eingabe-Fenster zum Tippen), „Befehl“ (das, was du eintippst), „Projektordner“ (der Ordner mit deiner App).

## 🧭 Mini‑Fahrplan: Start, Stopp, Wiederkommen
- Öffnen: Terminal (Eingabe‑Fenster) starten.
- Ins Projekt: `cd /Pfad/zu/avatar-panel-generator` (in den Ordner wechseln).
- Entwicklung starten: `npm run dev` (Start der App im Entwicklungsmodus).
- Stoppen: Drücke `Ctrl + C` (Abbrechen) im Terminal.
- Wiederkommen: Terminal öffnen → `cd` ins Projekt → `npm run dev` starten.
- Mit dem Assistenten sprechen: Starte den Codex‑Assistenten (Assistent im Terminal). Wenn du unsicher bist, frag: „Wie starte ich dich?“ – ich führe dich durch.

Es gibt keinen „Login“ (Anmeldung). „Ausloggen“ (Abmelden) = Terminal schließen oder `Ctrl + C` und `exit` (Beenden) tippen. „Einloggen“ (Anmelden) = Terminal öffnen, ins Projekt gehen, starten.

## 🌿 Wenn etwas zu viel ist
- Du darfst pausieren. Atme. Nichts geht kaputt.
- Wenn das Terminal „voll“ wirkt: `clear` (Anzeige leeren). Du siehst wieder ruhig den Anfang.
- Wenn etwas festhängt: `Ctrl + C` (Abbrechen). Danach neu starten.

## 🔑 6 Befehle, die reichen
- `ls` – Inhalt anzeigen (zeigt Ordner/Dateien hier)
- `cd Ordnername` – in Ordner wechseln (gehen)
- `cd ..` – einen Ordner hoch (zurück)
- `pwd` – wo bin ich? (aktueller Pfad)
- `clear` – Bildschirm leeren (aufgeräumt fühlen)
- Pfeil ↑ / ↓ – vorherige Befehle holen (Zeit sparen)

Zusätzlich hilfreich:
- `open .` – Ordner im Finder öffnen (am Mac)
- `code .` – Ordner in VS Code öffnen (Editor starten)

## 🚀 Deine App starten
1) Terminal öffnen (Eingabe‑Fenster).
2) Ins Projekt: `cd /Pfad/zu/avatar-panel-generator` (Ordner wechseln).
3) Dependencies installiert? Wenn nicht: `npm install` (benötigte Pakete holen).
4) Start: `npm run dev` (Entwicklungsserver starten).
5) Öffne den Link im Terminal, z. B. `http://localhost:5173/` (deine App).

Stoppen: `Ctrl + C` (Abbrechen) im Terminal.

## 💬 Mit dem Assistenten arbeiten
- Der Assistent lebt im Terminal (Assistent im Terminal). Er kann Dateien ändern (Code anpassen) und Befehle ausführen (Kommandos starten).
- Starten: Öffne das Terminal, gehe ins Projekt (`cd ...`), starte den Assistenten so wie du es sonst tust (z. B. über deinen gewohnten Befehl). Wenn unklar: Frag einfach im Terminal: „Wie starte ich dich?“
- Beenden: `Ctrl + C` (Abbrechen) oder `exit` (Beenden) → Fenster kann geschlossen werden.

## 🧰 Häufige Situationen
- „Ich habe mich verlaufen.“ → `pwd` (wo bin ich?) und `ls` (was ist hier?).
- „Falscher Ordner.“ → `cd ..` (hoch) oder `cd name` (rein) – wiederholen bis du im Projekt bist.
- „Zu viele Meldungen.“ → `clear` (Bildschirm leeren) – du startest „frisch“.
- „Etwas hängt.“ → `Ctrl + C` (Abbrechen) → Befehl neu ausführen.
- „Ich will neu starten.“ → `Ctrl + C`, dann wieder `npm run dev`.

## 🤝 Erlaubnis, es leicht zu machen
- Du musst nichts auswendig können – dieser Spickzettel ist immer da.
- Fehler sind okay – `Ctrl + C`, `clear`, noch einmal probieren.
- Fragezeichen sind willkommen – schreibe deine Frage einfach in den Chat.

## 📍 Wo ist dieser Spickzettel?
- Datei: `docs/TERMINAL-CHEATSHEET.md`
- Du findest ihn im Projektordner unter `docs/`.

Wenn du möchtest, kann ich dir auch eine gedruckte Mini‑Karte (PDF) bauen – sag einfach Bescheid.
